# PR #643 independent audit

Date: 2026-09-01

Scope: issue #619's backup directory-sync trace, per-barrier failure injection, and authoritative publication boundary.

## Verdict

PASS. Backup publication now uses a hidden pending name until the complete staging tree and stable parent directories have been synchronized. No injected pre-publication directory-barrier failure exposes a partially authoritative backup.

## Evidence

- The complete staging directory tree is synchronized bottom-up before either rename.
- The first rename publishes only a dot-prefixed `.pending-*` directory, which `list_backups` excludes from authoritative results.
- The `backups/` and data-root parent barriers complete before pending is renamed to the final visible backup name.
- Failure of the final `backups/` barrier has an explicitly outcome-unknown publication result, but any visible final directory contains a complete, pre-synchronized payload that passes manifest verification.
- The trace test records all 12 real directory barriers in their actual order.
- The failure-injection test fails each barrier in turn: barriers before final publication leave the authoritative list empty, while final-barrier failure can expose only a complete and verifiable backup.

## Validation

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed.
- `cargo test --workspace --locked --quiet`: passed; main engine target reported 1,578 passed and 5 ignored, with all other workspace targets passing.
- Sacred invariant `subagent_context_has_no_orchestrator_noise`: passed within the workspace suite.
- `git diff --check`: passed.

## Follow-up

- The audit's documentation correction was implemented in this PR: `list_backups` now documents that all dot-prefixed staging and pending directories are skipped.
- Non-blocking: consider a stable API/error classification for the final-barrier outcome-unknown result so callers can tell users to refresh and verify the backup list.
- Non-blocking: design bounded startup cleanup for abandoned hidden `.staging-*` and `.pending-*` directories; automatic deletion was intentionally not added without a defined safety policy.
