# PR #640 independent audit

Date: 2026-09-01

Scope: issue #621's literal pre-Workspace backup manifest v1 fixture and compatibility test.

## Verdict

PASS. The first independent review blocked an impossible historical version/source/scope combination and a test that did not reject current-only JSON fields. Both findings were fixed before this final verdict. No blocking or non-blocking findings remain.

## Compatibility evidence

- The fixture is static JSON loaded with `include_bytes!`; it is not emitted by the current serializer.
- `engine_version: 0.1.0` and `manual` / `full` match a backup that the producer at `d5abddb3^` could create.
- The fixture models `a.txt` containing `hello`; its SHA-256 and `AIRP-TREE-SHA256-v1` digest were independently checked and then verified through `verify_against_disk`.
- Exact key sets are asserted for the top-level manifest, source, scope, and file item, so current-only fields cannot silently enter the legacy specimen.

## Validation

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed.
- `cargo test --workspace --locked`: passed; main engine target reported 1,574 passed and 5 ignored, with all other workspace targets passing.
- Sacred invariant `subagent_context_has_no_orchestrator_noise`: passed within the workspace suite.
- `git diff --check`: passed.

## Follow-up

None. This audit produced no unimplemented recommendation requiring a post-merge issue.
