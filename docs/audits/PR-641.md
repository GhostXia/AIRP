# PR #641 independent audit

Date: 2026-09-01

Scope: issue #609's regression proof that corrupt Workspace integrity revokes a stale accepted Surface's first-party write authority.

## Verdict

PASS. The test creates a valid accepted session Surface, corrupts the committed Workspace bytes without changing its revision, and proves a valid `memory.replace` request is rejected before domain mutation. No production code changes are required.

## Evidence

- The changed `workspace.json` no longer matches its approved manifest hash, and `WorkspaceService::read()` explicitly fails before the intent is sent.
- The public response is the stable redacted `internal_error` envelope; internal path and hash details are not exposed.
- Resident Memory content and its SHA-256 remain unchanged.
- The Workspace `current_revision` bytes remain unchanged.
- The exact last-known-good accepted Surface remains in the registry for display, but cannot authorize the write.

## Validation

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed.
- `cargo test --workspace --locked`: passed; main engine target reported 1,575 passed and 5 ignored, with all other workspace targets passing.
- Sacred invariant `subagent_context_has_no_orchestrator_noise`: passed within the workspace suite.
- `git diff --check`: passed.

## Follow-up

None. The auditor's only non-blocking suggestion—directly asserting the Workspace integrity read failure—was implemented before PR creation.
