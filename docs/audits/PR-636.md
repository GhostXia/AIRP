# PR #636 independent audit

Date: 2026-08-31

Scope: issue #629, documenting repeated `core.inventory` instance budget behavior.

## Independent review

Two independent audit agents compared the baseline and topical contract with the Engine Surface builder, Workspace reducer, Inventory manifest, TypeScript parser, Vue copy, and existing regression tests.

The first review blocked two stale or over-broad statements. The baseline still described PR 11a as unmerged, and the draft implied that `move_widget` could change the complete-projection winner. The baseline now records that implementation merged in PR #627 while the product slice remains a candidate. The contract now distinguishes layout-tree movement from `widgets` array order: `move_widget` cannot change the winner; closing and reopening an instance can because it changes array order.

The second review blocked describing a future dedicated reason as automatically additive. Inventory v0.2.0 uses a closed reason enum: the current TypeScript parser degrades an unknown reason to generic unavailable, but strict old-schema consumers reject it. The contract and issue #628 now require a consumer-first, versioned manifest/parser/fixture rollout and producer gating. Until then, Vue truthfully retains generic `不可用` copy.

Both reviewers re-audited the corrections and returned **PASS with no blocking findings**.

## Residual observations

No non-blocking audit observations remain. The future machine-contract work is already tracked by issue #628.

## Validation

- focused Engine repeated-Inventory budget regression
- Vue Emotion/Inventory parser and manifest tests
- release workflow documentation tests
- `git diff --check`

All commands passed.

## Verdict

**PASS — no blocking findings.**
