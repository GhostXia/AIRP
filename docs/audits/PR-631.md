# PR #631 independent audit

Date: 2026-08-30

Scope: issue #178, the subagent decision-input boundary and selected tool-result grounding.

## Independent review

Two independent audit agents reviewed the implementation against the issue, provider wire formats, prompt assembly, trace provenance, token budgeting, and the sacred subagent-context invariant. Both returned **PASS with no blocking findings** after re-review.

The reviews did not treat the implementation or its documentation as authoritative. They inspected the changed code and tests independently and required the following corrections before passing:

- make the scoped assignment an explicit trusted generation instruction instead of describing it as data-only;
- preserve selected evidence as data-only and prevent it from granting tool authority;
- verify the final Persona/OpenAI-compatible and Anthropic payload shapes, not only intermediate structures;
- keep assignment and evidence trace positions byte-accurate for Unicode input;
- reject ambiguous planner output and unknown, duplicate, excessive, or unprojected evidence selections;
- charge assignment and evidence to the input budget before prompt preparation can cause persistence side effects.

All blocking findings were corrected and re-reviewed.

The repository audit bot then identified one additional blocking edge case: an absent run token budget was represented internally as `u64::MAX`, and subtracting assignment/evidence tokens made it look finite before the provider output cap was applied. The cap now receives the original optional budget state, leaves provider limits unchanged for an absent budget, and has a focused regression test.

## Residual observations

One non-blocking maintenance observation remains: keep test-fixture wording aligned with the production decision-input wording when that wording changes. This is not a missing behavior or a separate product defect; the current fixtures assert the required semantics and payload boundaries. No follow-up issue is required.

## Validation

- `cargo test --workspace --locked`
- `cargo fmt --all -- --check`
- `cargo clippy -p airp-core --all-targets --locked -- -D warnings`
- `cargo test -p airp-core --lib agent::tests:: --locked`
- `cargo test -p airp-core --test agent_run --locked`
- focused unlimited-budget regression test

All commands passed. The focused agent tests include the sacred `subagent_context_has_no_orchestrator_noise` invariant.

## Verdict

**PASS — no blocking findings.**
