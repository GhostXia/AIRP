# PR #298 独立审计报告 — Baseline 校准 + Agent 浏览器探索测试层实施计划

- **审计源模型**：GLM-5.2（Trae IDE，按根 `AGENTS.md` §Audit Agent Charter 独立执行）
- **审计日期**：2026-07-23
- **审计 charter**：根 `AGENTS.md` §Audit Agent Charter（独立审计 / 可提己见 / 可质疑历史并查证）
- **PR**：[#298 docs: baseline-calibration-2026-07-23](https://github.com/GhostXia/AIRP/pull/298)
- **分支**：`docs/baseline-calibration-2026-07-23` → `main`
- **base commit**：`03ffaf6 feat(webui): expose delete/search/persona-unbind operations (#296 B4/B11-B14, #295) (#297)`
- **PR 原始范围**：6 files, +1776/-30
  - 新增 `docs/AGENT-BROWSER-EXPLORATION-PLAN.md`（1756 行，issue #273 的 MVP 实施计划）
  - 校准 `docs/CURRENT-BASELINE.md`、`docs/WEBUI-PRODUCTION-PLAN.md`、`docs/SESSION-DATA-DESIGN.md`、`docs/PLAN.md`、`README.md` 至 `main@03ffaf6` 基线
- **审计后范围**：同 6 files（仅文档修订，无新增/删除文件）
- **审计 commits**（本分支追加）：见本报告最末"审计提交"段

## 1. 范围与背景

PR #298 是文档型 PR，核心交付物是 issue #273（Agent 驱动浏览器探索测试层）的 MVP 实施计划 `docs/AGENT-BROWSER-EXPLORATION-PLAN.md`，并配套把基线文档同步到 `main@03ffaf6` 状态。

由于本 PR 不含代码改动，审计重点为：

1. 实施计划自身作为"可直接执行的工程合同"的可执行性（计划即合同，文档错误会直接传到实施代码）；
2. 计划中嵌入的 JavaScript / YAML / shell 代码片段的正确性（这些片段会被 Task 实施时原样落地）；
3. CSP / 安全 / 数据完整性等仓库硬约束在计划中是否被正确尊重；
4. 基线校准文档与 `main` 当前事实是否一致。

CodeRabbit 在本 PR 上提了 18 条 review thread。本审计按 charter 三原则独立复核每一条，不附和、不照搬，并补充 CodeRabbit 未覆盖的发现。

## 2. 独立证据

### 2.1 CodeRabbit 18 条 findings 复核结论

| # | CodeRabbit 指控 | 独立复核结论 | 处理 |
|---|---|---|---|
| F1 | 基线未 pin 到具体 commit | 属实（line 7 只写日期） | 已修：补 `main @ 03ffaf6` |
| F2 | Task 2 增量测试片段重复 `import { readFile }` | 属实（与文件顶部 import 冲突） | 已修：改为注释提示，删除重复 import |
| F3 | `import.meta.env` 在 classic `<script>` 中不可用 | 属实（CSP + classic script 限制） | 已修：改用 `globalThis.VITE_AIRP_AGENT_TEST` 运行时探测 |
| F4 | `clickButton` 用文本作为 `querySelector` 会抛语法错误 | 属实（如"发送首轮消息"非合法选择器） | 已修：先按 button 文本匹配，回落到 try/catch 选择器 |
| F5 | `HarnessClient.navigate` 用 `window.location.href` 与 Playwright `page` 重复且无 origin | 属实（race + 无 origin） | 已修：改用 `page.goto(origin + path)` + `waitForReady` |
| F6 | `package.json` 缺 `test` script | 属实 | 已修：补 `"test": "node --test classifier.test.mjs"` |
| F7 | `chatCompletion` 无 timeout，provider 卡死会挂死 CI | 属实 | 已修：`AbortController + setTimeout(timeoutMs)` |
| F8 | `HarnessClient` 构造器无 origin 参数，`navigate` 拼接相对路径 | 属实（与 F5 同源） | 已修：constructor 接 `origin`，navigate 用 `new URL(origin + path)` |
| F9 | `waitFor` 用 `new Function(predicateSrc)` 违反 CSP `unsafe-eval` | 属实 | 已修：改用 `PREDICATES` 注册表 + `predicateId` 标识符 |
| F10 | `taskNames = Object.keys(...).flatMap(...)` 误用 flatMap | 属实（返回二维数组） | 已修：`[...new Set(taskNames)]` |
| F11 | `lastScriptContent` 在循环外未声明，`finally` 中引用 undefined | 属实 | 已修：循环前 `let lastScriptContent = ''` |
| F12 | DOM 快照未脱敏 message-like 元素，可能泄露 RP 内容给 LLM | 属实 | 已修：新增 `sanitizeDomSnapshot` helper，按 id/classes/role 匹配 `message|msg|chat|memory|history|conversation|reply|content` 正则脱敏 |
| F13 | `runTempScript` 通过 `import()` 继承 `process.env`，泄露 OPENAI_API_KEY 等给 LLM 生成脚本 | 属实（Agent 生成的临时脚本不应信任） | 已修：scrub `SECRET_PATTERNS` 匹配的 env var，finally 恢复 |
| F14 | runner 通过 `--fixtures` 命令行传角色卡 JSON 路径，LLM prompt 中暴露本机路径 | 属实（与"secret 不进 prompt"原则一致） | 已修：改用 `ctx.fixtures.characterCard` 直接传 JSON 对象，prompt 中只引用 `ctx.fixtures.characterCard` |
| F15 | Install Chrome step 无 `id`，但 `steps.setup-chrome.outputs.chrome-path` 被引用 | 属实 | 已修：加 `id: setup-chrome` |
| F16 | `./smoke-ci.sh & ; wait $SMOKE_PID` 会跑完整 smoke 而非只启拓扑 | 属实 | 已修：改为 `bootstrap-topology.sh`（启拓扑后返回）+ `Teardown topology` step（`if: always()`） |
| F17 | "Publish PR comment" step 误设 `working-directory: tools/agent-exploration` | **不属实（误报）** | 拒绝：经独立读 YAML（line 1646-1658），该 step 无 `working-directory` 字段，命令 `node tools/agent-exploration/post-pr-comment.mjs ...` 用相对仓库根路径正确 |
| F18 | `cursor"加载更早"` 措辞在 CURRENT-BASELINE.md 和 WEBUI-PRODUCTION-PLAN.md 中未规范化 | 属实（中文排版缺空格） | 已修：`cursor-based "加载更早"` |

**独立复核统计**：18 条中 **17 条属实**（已修）、**1 条误报**（F17，拒绝）。

### 2.2 CodeRabbit 未覆盖的独立发现

#### I1 — `action-protocol.mjs` 中 `'wait'` action 注释仍写 `predicateSrc`（与 F9 修复不一致）

`docs/AGENT-BROWSER-EXPLORATION-PLAN.md` line 652 在 SUPPORTED_ACTIONS 注释中写 `target: predicateSrc`，暗示接收源代码字符串供 `new Function` 编译——但 F9 已把 harness 内的 `waitFor` 改为基于 `predicateId` 的 PREDICATES 注册表。两处不一致会让 Plan B 实施者按"源代码字符串"接口设计执行器，再次撞 CSP `unsafe-eval`。

**严重度**：低（Plan B 是预留接口，MVP 不实现执行器）。
**处理**：已修：注释改为 `target: predicateId`，与 harness 实现对齐。

#### I2 — `bootstrap-topology.sh` 在计划中只占位、未给代码，但 workflow 已直接调用

F16 修复后，CI workflow 直接 `./bootstrap-topology.sh`，但计划文档对该脚本只给"从 `smoke-ci.sh` 抽取"的描述，未给代码。这是"计划即合同"模式下可接受的省略（Task 9 Step 1 已注明"实施时必须先做这步"），但需明确：**实施 Task 9 Step 1 时，`bootstrap-topology.sh` 必须先于 workflow 文件落地**，否则 CI 会失败。

**严重度**：信息（非阻塞，已在注意中说明）。
**处理**：保留注意原文，并在审计报告中强调时序约束。

### 2.3 关于 F17 误报的独立证据

CodeRabbit 声称"Publish PR comment" step 设了 `working-directory: tools/agent-exploration`。我独立读 YAML 验证：

```yaml
      - name: Publish PR comment
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          node tools/agent-exploration/post-pr-comment.mjs artifacts/agent-exploration/report.md
```

该 step **没有 `working-directory` 字段**，使用 workflow 默认（仓库根）。命令中 `node tools/agent-exploration/post-pr-comment.mjs artifacts/agent-exploration/report.md` 用相对仓库根的路径正确解析。CodeRabbit 把其他 step 的 `working-directory` 误读到了 Publish step 上。

按 charter §1（独立审计，不附和），本条拒绝，不修。

## 3. 修复总览

### 3.1 修改文件清单

| 文件 | 改动 | 涉及 findings |
|---|---|---|
| `docs/AGENT-BROWSER-EXPLORATION-PLAN.md` | 16 处编辑 | F1-F16 + I1 |
| `docs/CURRENT-BASELINE.md` | 1 处编辑 | F18 |
| `docs/WEBUI-PRODUCTION-PLAN.md` | 1 处编辑 | F18 |

### 3.2 按 finding 的修复要点

- **F1（基线 pin commit）**：line 7 改为 `> 基线日期：2026-07-23，\`main\` 分支 @ \`03ffaf6\``
- **F2（重复 import）**：Task 2 增量测试片段删除 `import { readFile } from 'node:fs/promises';`，改为注释"readFile 已在文件顶部导入，不重复 import"
- **F3（import.meta.env 不可用）**：harness 的 `shouldInstallAgentTestHarness()` 改为运行时探测 `globalThis.VITE_AIRP_AGENT_TEST === '1'`，并加注释说明 classic `<script>` 无 `import.meta.env`
- **F4（clickButton 文本作选择器）**：先 `Array.from(document.querySelectorAll('button, [role="button"]'))` 按文本匹配，未命中再 try/catch 内 `querySelector`
- **F5 + F8（HarnessClient origin + page.goto）**：constructor 接 `origin` 参数，navigate 用 `new URL(this.origin + '/screens/' + screen)` 拼接，调 `page.goto(url.href, { waitUntil: 'load' })`，并新增 `waitForReady` 轮询 `isReady()` 直到 harness 安装
- **F6（package.json test script）**：`"scripts": { "test": "node --test classifier.test.mjs", "run": "node runner.mjs" }`
- **F7（chatCompletion timeout）**：`AbortController + setTimeout(timeoutMs=60000)`，捕获 `AbortError` 转友好错误
- **F9（waitFor new Function 违 CSP）**：harness 与 HarnessClient 双侧改为 `PREDICATES` 注册表（`'send-button-ready'` / `'no-pending-request'` / `'message-list-stable'`）+ `predicateId` 标识符
- **F10（flatMap 误用）**：`taskNames = [...new Set(taskNames)]`
- **F11（lastScriptContent 未声明）**：循环前 `let lastScriptContent = ''`
- **F12（DOM 快照未脱敏）**：新增 `sanitizeDomSnapshot(snapshot)` helper，按 `message|msg|chat|memory|history|conversation|reply|content` 正则匹配元素 scope，命中则 `text: '[REDACTED]'`
- **F13（runTempScript env 泄露）**：`SECRET_PATTERNS = [/OPENAI_API_KEY/i, /GITHUB_TOKEN/i, /API_KEY/i, /SECRET/i, /PASSWORD/i, /TOKEN/i, /_KEY$/i]`，调用前 save+delete 命中 key，finally 恢复
- **F14（fixture 路径泄露）**：`FIXTURES = { characterCard: JSON.parse(await readFile(cardPath, 'utf8')) }`；runner 构造 `ctx = { page, harness, context, origin, fixtures: mod.FIXTURES || {} }`；prompt 中只引用 `ctx.fixtures.characterCard`，不暴露本机路径
- **F15（setup-chrome id）**：Install Chrome step 加 `id: setup-chrome`
- **F16（smoke-ci & wait 重复跑 smoke）**：改为 `bootstrap-topology.sh`（启拓扑不跑 smoke，等待 health ready 后返回）+ `Teardown topology` step（`if: always()`，`./bootstrap-topology.sh --teardown`）；同步更新注意 1
- **F18（cursor 措辞）**：`cursor"加载更早"` → `cursor-based "加载更早"`，在 CURRENT-BASELINE.md 和 WEBUI-PRODUCTION-PLAN.md 两处同步修
- **I1（action-protocol predicateSrc 残留）**：`'wait'` action 注释 `target: predicateSrc` → `target: predicateId`

### 3.3 拒绝修复

- **F17**：经独立读 YAML 验证为误报，拒绝（见 §2.3）。

## 4. 独立审计（CodeRabbit 之外）

按 charter §2（可提己见），对计划文档自身的设计判断：

### 4.1 计划架构判断

- **Plan A（生成临时 Playwright 脚本）+ Plan B（action protocol，预留）** 的双轨设计合理：MVP 用 Plan A 快速验证价值，Plan B 留待阶段 3 接入以收紧安全边界。审计无异议。
- **harness 双层模型**（in-page `window.__AIRP_AGENT_TEST__` + Node 侧 `HarnessClient`）与现有 webui vanilla JS 多屏导航模型一致，不引入构建步骤，与仓库"无构建 WebUI"边界对齐。审计无异议。
- **CI 非阻塞** 符合 issue #273 阶段 1 定位（"MVP 不阻塞 PR"）。审计无异议。

### 4.2 计划可执行性判断

- 计划中所有 JavaScript / YAML / shell 片段经本次审计修复后，可直接落地。
- `bootstrap-topology.sh` 是计划中唯一"占位但不展开代码"的脚本。这是"计划即合同"模式下可接受的省略，但实施时必须先于 workflow 落地。审计已在 §2.2-I2 中标记时序约束。
- 计划明确"桌面 `ui/` harness 升级推迟到 issue #273 评论中显式写明此决定"（line 61），与 charter §3（可质疑历史）一致——审计认为这是诚实的取舍，而非隐藏范围。

### 4.3 安全边界判断

经 F3 / F9 / F12 / F13 / F14 修复后，计划在以下安全边界与仓库硬约束对齐：

- **CSP `unsafe-eval` 禁止**：harness 不用 `new Function` / `eval`，waitFor 改为 predicateId 注册表（F3/F9/I1）。
- **secret 不进 prompt / 不进日志**：fixture 通过 `ctx.fixtures` 传 JSON 对象（F14），runTempScript scrub `process.env` 中的 secret key（F13），DOM 快照脱敏 message-like 元素（F12）。
- **不复制第三方资产**：计划 §"文件结构" 明确 `fixtures/character-card.json` 为合成 fixture，并要求"不复制第三方资产"（line 97），与 charter "第三方经验吸收与独立实现" 一致。

### 4.4 基线校准文档判断

CURRENT-BASELINE.md 与 WEBUI-PRODUCTION-PLAN.md 经 F18 修复后，与 `main@03ffaf6` 当前事实一致。审计未发现其他失同步项。

## 5. 阻塞结论

**PASS（有条件）**：本 PR 在应用本审计的 17 处修复后，可作为 issue #273 MVP 实施的工程合同合并。条件：

1. 本审计 commits 合入 PR 分支；
2. 实施 Task 9 Step 1 时，`bootstrap-topology.sh` 必须先于 workflow 落地（见 §2.2-I2）。

CodeRabbit F17 为误报，不阻塞。本审计无额外阻塞意见。

## 6. 审计遗留项（PR 合并后转 issue）

按 charter "审计遗留项处理"（PR 合并后写入 GitHub issue）：

- 无。本审计所有属实 findings（F1-F16 + F18 + I1）均已在 PR 分支内修复，无未改动但需后续跟进的修改意见。

## 7. 审计提交

本审计在 `docs/baseline-calibration-2026-07-23` 分支上追加单个 commit，包含：

- `docs/AGENT-BROWSER-EXPLORATION-PLAN.md`（16 处编辑）
- `docs/CURRENT-BASELINE.md`（1 处编辑）
- `docs/WEBUI-PRODUCTION-PLAN.md`（1 处编辑）
- `docs/audits/PR-298.md`（本报告，按仓库惯例随 PR 归档）

Commit message：`docs: fix PR #298 audit findings (17 of 18 CodeRabbit findings, #17 rejected as false positive)`
