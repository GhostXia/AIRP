# PR #384 内部 pre-merge 评审（2026-07-30，2026-07-31 更新）

> 评审角色：**开发 agent 自审 + CI 失败复盘 + CodeRabbit 自动化复审**（合并前）。  
> **非独立审计**：本文不是 §11.1「PR → 审计 → 合并」门禁下的独立审计 bot 产物；独立审计仍须由审计 bot 在 PR 合并前完成，并遵守 AGENTS.md 审计守则三原则。  
> 基线分支：`codex/engine-v003-ep0-dns-conversation-freeze`  
> 范围：E-P0-2 Conversation 冻结文档 + E-P0-3 plugin webhook DNS fail-closed/pin  
> 评审提交：`b2557ac`（初版）→ `063a1c9`（A1–A4 修复）→ `fc3a498`（CR1–CR6 + N2/N3 修复）

## 1. 裁决

**条件通过（尚未 Approve 合并）**。初版 CI 红（A1–A4）已修；2026-07-31 CodeRabbit 复审发现
CR1（critical SSRF bypass）等 6 项，全部已修。N1/N4 经核验无需修改。

合并前仍须满足以下 pending 检查（见 §3 结论）：
1. `cargo test --workspace --locked` CI 全绿（本评审时 Rust test 仍 IN_PROGRESS）；
2. 审计 bot 独立审计通过（§11.1 门禁）；
3. 人工 review 批准。

## 2. 评审判断

### 通过点

1. **E-P0-2 决策 B** 写进基线 §5.0 / PLAN §2.1：产品主路径仍是 legacy Chat，Conversation 冻结对称扩面。
2. **DNS fail-closed** 方向正确：解析失败/空答案/任一内网地址拒绝。
3. **请求时重检 + resolve_to_addrs pin** 缩小 rebinding 窗口。
4. **lookup 可注入** 使 fail-closed 分支可测。

### 阻塞 / CI 失败（初版，已修）

| ID | 严重度 | 问题 | 处理 | 验证证据 |
|---|---|---|---|---|
| A1 | Blocker | rustdoc 公开 API 链到私有 inspect | 去掉私有 intra-doc link | CI `Rust doc` SUCCESS（fc3a498）；本地 `cargo doc --workspace --no-deps` |
| A2 | Blocker | clippy io_other_error | `Error::other` | CI `Rust lint (fmt + clippy)` SUCCESS（fc3a498） |
| A3 | Blocker | accepts_https 依赖 api.example.com 真实 DNS | 公网字面量 + inject pin | `cargo test -p airp-core --locked plugin_tool`（本地 31 passed，含 inject pin 路径）；CI `Rust test` 待全绿 |
| A4 | Blocker | conversation slow_append flaky 100ms | 延迟 750ms（后 CR5 改为 Notify 确定性信号） | `cargo test -p airp-core --locked conversation`（本地通过）；CI `Rust test` 待全绿 |

### CodeRabbit 复审（2026-07-31，全部已修）

| ID | 严重度 | 问题 | 处理 | 验证证据 |
|---|---|---|---|---|
| CR1 | 🔴 Critical | `is_internal_ip` 不拆解 IPv4-mapped (`::ffff:127.0.0.1`) / compatible IPv6，可绕过 SSRF 拒绝 | V6 分支先 `to_ipv4_mapped()` 拆解，再检查 compatible；递归复用 IPv4 判定 | `cargo test -p airp-core --locked plugin_tool`（含 `https://[::ffff:127.0.0.1]` / `[::127.0.0.1]` 拒绝 assert）；CI `Rust test` 待全绿 |
| CR2 | — | 补 `https://[::ffff:127.0.0.1]` / `[::127.0.0.1]` 拒绝测试 | 已补 4 条 assert | 同 CR1 测试命令 |
| CR3 | — | `call_webhook` 执行路径 `inspect_webhook_url` 同步 DNS 阻塞 async runtime | `spawn_blocking` 包裹 | CI `Rust lint` SUCCESS（无 clippy 阻塞告警） |
| CR4 | Nitpick | 请求时 DNS 失败误归 `BadRequest`(400) | 改 `Internal`（与超时/发送失败一致） | 代码 review（`engine/src/plugin_tool.rs` call_webhook 错误分支） |
| CR5 | — | `slow_append` 测试盲 sleep 非确定性 | `ConversationIoFault::Delay` 携带 `Arc<Notify>`，阻塞前 `notify_one()`，fast 任务 `notified().await` | `cargo test -p airp-core --locked conversation`（本地确定性通过） |
| CR6 | — | `CURRENT-BASELINE.md` Plugin DNS 条目未用删除线约定 | 改为 `~~...~~ → **已落地**` | 人工核验 `docs/CURRENT-BASELINE.md` §5.0 |

> 说明：上表「验证证据」列中标注「本地通过」者尚未经 CI 独立复跑确认；CI `Rust test` 全绿前，相关修复应视为「已实现但尚未独立验证」。

### 非阻塞

| ID | 说明 | 处理 | 验证证据 |
|---|---|---|---|
| N1 | one-shot pin client 重建连接池，插件低频可接受 | 不修（评审已接受；加缓存引入 stale-DNS 风险，反损 per-request re-resolve 目标） | 设计取舍记录；记为已接受遗留风险（见 §3） |
| N2 | 注册路径同步 DNS 可能卡顿 | **已修**：`upsert_plugin_tool_endpoint` 的 `config.validate()` 改 `spawn_blocking`（同 CR3 思路） | CI `Rust lint` SUCCESS；`Rust test` 待全绿 |
| N3 | E-P0-2 无 CI 防 Conversation 扩面 | **已修**：新增 `engine/tests/conversation_freeze_check.ps1` + 基线文件，挂 pr-gate CI | CI `Production topology` SUCCESS（含 freeze guard 步骤） |
| N4 | 不得宣称 plugin 多租户沙箱 | 不修（已核验 CURRENT-BASELINE / SECURITY / RISK-REGISTER / engine README 一致声明 "trusted-user extensions, not a sandbox"，无虚假声明） | 人工交叉核验四份文档 |

## 3. 结论

**条件通过**：A1–A4 + CR1–CR6 + N2 + N3 已实现，N1/N4 经核验无需修改。

合并前仍须满足以下 pending 检查（任一未过不得合并）：
1. **CI `Rust test` 全绿**：`cargo test --workspace --locked` 在 `fc3a498` 上通过（本评审时仍 IN_PROGRESS；其余 CI 检查已 SUCCESS）；
2. **审计 bot 独立审计通过**：按 §11.1 门禁，审计 bot 须独立完成审计并裁决阻塞意见（本文不替代该门禁）；
3. **人工 review 批准**：本地全绿 + 审计 bot 通过后，仍由人工 review 决定是否合并。

合并后遗留项（已接受遗留风险）：
- **N1**：one-shot pin client 重建连接池，记为已接受设计取舍（插件低频调用，加缓存反损 per-request re-resolve 目标）。不另开 issue。
- N4 为已满足的诚实性约束，无遗留。

#381 不整关；下一步 #284。
