# PR #384 独立审计（2026-07-30，2026-07-31 更新）

> 审计角色：开发 agent 自审 + CI 失败复盘（合并前）；2026-07-31 补 CodeRabbit 复审  
> 基线分支：`codex/engine-v003-ep0-dns-conversation-freeze`  
> 范围：E-P0-2 Conversation 冻结文档 + E-P0-3 plugin webhook DNS fail-closed/pin

## 1. 裁决

**条件通过 → 修复后可合并**。初版 CI 红（A1–A4）已修；2026-07-31 CodeRabbit 复审发现
CR1（critical SSRF bypass）等 6 项，全部已修。N1/N4 经核验无需修改。

## 2. 独立判断

### 通过点

1. **E-P0-2 决策 B** 写进基线 §5.0 / PLAN §2.1：产品主路径仍是 legacy Chat，Conversation 冻结对称扩面。
2. **DNS fail-closed** 方向正确：解析失败/空答案/任一内网地址拒绝。
3. **请求时重检 + resolve_to_addrs pin** 缩小 rebinding 窗口。
4. **lookup 可注入** 使 fail-closed 分支可测。

### 阻塞 / CI 失败（初版，已修）

| ID | 严重度 | 问题 | 处理 |
|---|---|---|---|
| A1 | Blocker | rustdoc 公开 API 链到私有 inspect | 去掉私有 intra-doc link |
| A2 | Blocker | clippy io_other_error | Error::other |
| A3 | Blocker | accepts_https 依赖 api.example.com 真实 DNS | 公网字面量 + inject pin |
| A4 | Blocker | conversation slow_append flaky 100ms | 延迟 750ms（后 CR5 改为 Notify 确定性信号） |

### CodeRabbit 复审（2026-07-31，全部已修）

| ID | 严重度 | 问题 | 处理 |
|---|---|---|---|
| CR1 | 🔴 Critical | `is_internal_ip` 不拆解 IPv4-mapped (`::ffff:127.0.0.1`) / compatible IPv6，可绕过 SSRF 拒绝 | V6 分支先 `to_ipv4_mapped()` 拆解，再检查 compatible；递归复用 IPv4 判定 |
| CR2 | — | 补 `https://[::ffff:127.0.0.1]` / `[::127.0.0.1]` 拒绝测试 | 已补 4 条 assert |
| CR3 | — | `call_webhook` 执行路径 `inspect_webhook_url` 同步 DNS 阻塞 async runtime | `spawn_blocking` 包裹 |
| CR4 | Nitpick | 请求时 DNS 失败误归 `BadRequest`(400) | 改 `Internal`（与超时/发送失败一致） |
| CR5 | — | `slow_append` 测试盲 sleep 非确定性 | `ConversationIoFault::Delay` 携带 `Arc<Notify>`，阻塞前 `notify_one()`，fast 任务 `notified().await` |
| CR6 | — | `CURRENT-BASELINE.md` Plugin DNS 条目未用删除线约定 | 改为 `~~...~~ → **已落地**` |

### 非阻塞

| ID | 说明 | 处理 |
|---|---|---|
| N1 | one-shot pin client 重建连接池，插件低频可接受 | 不修（审计已接受；加缓存引入 stale-DNS 风险，反损 per-request re-resolve 目标） |
| N2 | 注册路径同步 DNS 可能卡顿 | **已修**：`upsert_plugin_tool_endpoint` 的 `config.validate()` 改 `spawn_blocking`（同 CR3 思路） |
| N3 | E-P0-2 无 CI 防 Conversation 扩面 | **已修**：新增 `engine/tests/conversation_freeze_check.ps1` + 基线文件，挂 pr-gate CI |
| N4 | 不得宣称 plugin 多租户沙箱 | 不修（已核验 CURRENT-BASELINE / SECURITY / RISK-REGISTER / engine README 一致声明 "trusted-user extensions, not a sandbox"，无虚假声明） |

## 3. 结论

修复 A1–A4 + CR1–CR6 + N2 + N3 后 **Approve 合并**。N1/N4 经独立核验无需修改。
合并后遗留项（无）：N1 为已接受的设计取舍，N4 为已满足的诚实性约束。#381 不整关；下一步 #284。
