# PR #648 independent audit

Date: 2026-09-01

Scope: issue #644's outcome-unknown classification for the final backup publication barrier.

## Verdict

PASS. Only failure of the final parent-directory barrier after the visible-name rename receives the structured publication-unknown result. Earlier failures remain definite and cannot expose an authoritative backup.

## Evidence

- Staging-tree and stable-parent barriers, plus both pre-publication renames, retain their original error types.
- The final `backups/` barrier maps to `BackupPublicationOutcomeUnknown` with the internally generated UUID backup ID.
- The public HTTP envelope returns stable code `backup_publication_outcome_unknown`, recovery `refresh_and_verify_backup`, and the backup ID while redacting paths and the underlying sync error.
- Failure injection covers every real directory barrier. The first 11 failures leave the authoritative list empty and never receive the publication-unknown classification.
- Final-barrier failure returns an ID matching the visible manifest, whose full file and tree verification succeeds; the implementation does not claim that the final name is crash-durable.
- Manual creation, restore rollback creation, and Workspace migration propagate the structured error without beginning their following mutation.
- Character/session pre-delete paths preserve this error while remaining fail-closed; ordinary backup failures still gain resource context.

## Validation

- Three targeted classification, envelope, and pre-delete propagation tests: passed.
- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed.
- `cargo test --workspace --locked --quiet`: passed; main engine target reported 1,584 passed and 5 ignored, with all other workspace targets passing.
- Sacred invariant `subagent_context_has_no_orchestrator_noise`: passed within the workspace suite.
- `git diff --check`: passed.

## Follow-up

None.
