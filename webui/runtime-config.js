// Development default. The production gateway image replaces this file with
// deploy/production/runtime-config.js so browsers use the authenticated origin.
window.AIRP_WEBUI_CONFIG = Object.freeze({ mode: 'development' });
