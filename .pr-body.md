## Summary

Engineering governance bundle closing #128, #192, #190. One PR, three issue-scoped commits + one drive-by clippy fix.

| Ref | Title |
|---|---|
| Closes #128 | Bump `actions/*` to Node 24 runtime majors |
| Closes #192 | Dependency discovery automation + audit routing |
| Closes #190 | Third-party notices + SBOM generation |

## Commits

1. `ci: bump actions/* to Node 24 runtime majors (#128)` — `actions/checkout@v4→v7`, `actions/setup-node@v4→v6`, `actions/upload-artifact@v4→v7`. All three verified via `action.yml` `runs.using: node24`. `node-version: "20.19.0"` retained (CI build Node, not action runtime). `dtolnay/rust-toolchain@stable` out of scope (#128 targets `actions/*` only; separate follow-up).
2. `fix(engine/fsm): collapse else-if to satisfy clippy::collapsible_else_if` — drive-by. Pre-existing warning on `main` was blocking the `-D warnings` gate. Mechanical `else { if let }` → `else if let`, no behavior change. Not in #217 forbidden list.
3. `feat(dep-governance): dependency discovery + audit routing (#192)` — `tools/dep-governance/` toolkit:
   - `audit-routing.mjs` (pure, no I/O): minimal semver parser, `classifyInventory` (auto-pass / audit-required / block), `classifyUpgrade` (5 classes + patch-sensitive override per DEV-GUIDE §7.1)
   - `discover-deps.mjs`: scans Cargo workspace + `ui/package-lock.json` v3, BFS scope classification (runtime > build > dev), atomic writes, sorted JSON output
   - `audit-routing.config.json`: license tiers, sensitive areas, dedup key format
   - `routing-dry-run.mjs`: 12/12 fixtures pass (all 5 classes + patch-sensitive + downgrade + same-version + unparseable)
   - 40 tests in `routing.test.mjs` + `discover.test.mjs`
4. `feat(dep-governance): SBOM + third-party notices generator (#190)` — `generate-sbom.mjs` consumes the #192 inventory and emits:
   - `docs/sbom/airp.spdx.json` (SPDX-2.3 JSON)
   - `docs/sbom/airp.cdx.json` (CycloneDX 1.5 JSON)
   - `docs/sbom/THIRD-PARTY-NOTICES.txt` (human-readable bundle)
   - 24 tests in `sbom.test.mjs`

## Independent implementation

Per AGENTS.md "第三方经验吸收与独立实现": scripts use **only Node built-ins** (`fs`, `path`, `crypto`, `node:test`, `node:assert`). No SBOM library, no semver library, no `@actions/*` runtime. SPDX/CycloneDX document structures are built from their public specs.

**No network access required.** No GitHub tokens, no npm tokens, no cargo tokens. Safe to run in air-gapped CI.

## Real-repo SBOM output

```
688 total records (4 first-party + 684 third-party)
661 auto-pass    (MIT, Apache-2.0, BSD-*, ISC, Zlib, …)
 27 audit-required  (MPL-2.0 ×14, LGPL-2.1-or-later ×2, complex SPDX expressions ×11)
  0 block           (no GPL/AGPL/SSPL/BUSL in runtime scope — release not blocked)
  9 unknown-to-SPDX (NOASSERTION; all actually permissive — see follow-up #1)
```

The 27 audit-required records are the **input list** for the next release audit cycle; they do not block this PR.

## Verification evidence (local, Linux sandbox)

| Command | Result |
|---|---|
| `node --test tools/dep-governance/tests/*.test.mjs` | 64/64 pass |
| `node tools/dep-governance/routing-dry-run.mjs` | 12/12 pass |
| `node --test webui/tests/*.test.mjs` | 69/69 pass |
| `cargo clippy --workspace --all-targets -- -D warnings` | 0 warnings |
| `cargo test --tests -p airp-core` | all suites pass (0 failures) |
| `cargo test -p airp-core --lib subagent_context_has_no_orchestrator_noise` | 1/1 pass (sacred invariant green) |

## Forbidden files (no conflict with #217)

Verified unchanged:
- `engine/src/chat_pipeline.rs`, `engine/src/chat_pipeline/tests.rs`, `engine/src/orchestrator/trace.rs`
- `webui/assembly-utils.js`, `webui/tests/assembly.test.mjs`, `ui/production-browser-smoke.mjs`
- `docs/CURRENT-BASELINE.md`, `docs/WEBUI-PRODUCTION-PLAN.md`

## Audit report

`docs/audits/` is gitignored (per repo policy: "本地审计/烟测原始输出只作临时诊断；可重复证据由 CI run 与历史归档索引保存"). The audit report is therefore inlined here:

> **Status: PASS — no blocking issues.** Independent review per AGENTS.md §"Audit Agent Charter".
>
> **Strengths:** clean separation (pure routing module + thin CLI wrappers); conservative-by-default (unknown → audit-required, never silent auto-pass; unrecognized SPDX → NOASSERTION, never guessed); deterministic output (sorted keys, sorted components, timestamp sourced from inventory); atomic writes (tmp + rename); independent implementation (Node built-ins only).
>
> **Weaknesses (non-blocking):** SPDX expression parser is simplistic (no parens / WITH-clause grouping / operator precedence → over-classifies, never under-classifies); live upgrade detector not wired (dry-run fixtures prove routing logic; live registry-querying detector is documented as deferred in DEV-GUIDE §7.1).
>
> **#128 compliance:** verified `actions/checkout@v7`, `setup-node@v6`, `upload-artifact@v7` all use `runs.using: node24` via their `action.yml`. `node-version: "20.19.0"` is the CI build Node (not action runtime) — correct.
>
> **AGENTS.md compliance:** independent implementation ✅; lockfile versions drive inventory ✅; license + provenance recorded ✅; `ACKNOWLEDGEMENTS.md` §3 sync deferred to formal release (non-blocking) ⚠️; sacred invariant untouched ✅; forbidden files untouched ✅.

## Non-blocking follow-ups (to be filed as GitHub issues after merge per AGENTS.md)

1. **SPDX expression parser limitations** — implement minimal independent parser handling parens / `WITH`-clauses / `AND`/`OR` precedence; add `MIT-0` and `Apache-2.0 WITH LLVM-exception` to auto_pass lists. Removes 9 false NOASSERTIONs and ~11 false audit-requireds.
2. **Live upgrade detector** — wire `classifyUpgrade` to query crates.io / npmjs.org for latest stable; dedup by `{ecosystem}:{name}@{target_version}`; never auto-merge.
3. **OS-level package SBOM** — enumerate Debian base image packages from `deploy/production/Dockerfile.engine`.
4. **SBOM timestamp determinism** — source `meta.generated_at` from `git log -1 --format=%cI` instead of `new Date()` so PR diffs don't show a one-line timestamp change.
5. **`dtolnay/rust-toolchain@stable` audit** — evaluate pinning vs replacing with `rustup` directly (third-party action, out of scope for #128).
6. **`ACKNOWLEDGEMENTS.md` §3 sync** — when next formal release tag is cut, copy the SBOM third-party records into §3 per the documented release procedure.

## How to regenerate

```bash
# Inventory (run from repo root)
node tools/dep-governance/discover-deps.mjs --repo-root . --out-dir docs/sbom

# SBOM + notices
node tools/dep-governance/generate-sbom.mjs --inventory docs/sbom/inventory.json --out-dir docs/sbom

# Tests
node --test tools/dep-governance/tests/*.test.mjs

# Routing dry-run
node tools/dep-governance/routing-dry-run.mjs
```

See `tools/dep-governance/README.md` for full operator docs.
